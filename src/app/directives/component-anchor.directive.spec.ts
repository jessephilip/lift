import { ComponentAnchorDirective } from './component-anchor.directive';

describe('ComponentAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
